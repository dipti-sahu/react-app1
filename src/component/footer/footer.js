import React from 'react';
import style from './footer.module.css'
const Footer = () => {
  return (
    <div className={`bg-primary text-center ${style.footer}`}>
      small family happy family
    </div>
  );
}

export default Footer;
